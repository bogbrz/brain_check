import 'package:flutter/material.dart';

class DuelPage extends StatelessWidget {
  const DuelPage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Text("Duel Page")),
    );
  }
}
